# v1.9.2

## Whats New

- Daemon VB sekarang memakai entry point worker `sync_all_stores`.

## Spesifikasi

- `main-vb/src/worker.py` tetap identik dengan `main-bot/src/worker.py`.
- Compatibility alias hanya berada di `main-vb/src/daemon.py`.

## Handling

- Mengatasi `ImportError: cannot import name 'patrol_once'` saat container
  `fm-bot-vb` start.
- Tidak mengubah worker bot-OC dan tidak menjalankan aksi Shopee saat validasi.
