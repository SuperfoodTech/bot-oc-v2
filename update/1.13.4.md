# Release Note 1.13.4

## Whats New
- Menyempurnakan subteks status pada fungsi `getOutletPauseLine` di Dashboard Mitra (`user_dashboard.html`) untuk fase antrean bot:
  - Fase `PENDING_PAUSE` (antrean tutup): `"Bot sedang dalam proses penutupan outlet"`.
  - Fase `PAUSE` aktif: Menampilkan waktu estimasi buka kembali (`formatPauseResumeInline`).
  - Fase `PENDING_OPEN` (antrean buka): `"Bot sedang dalam proses pembukaan outlet"`.

## Spesifikasi
- **Target File**:
  - `src/backend/templates/user_dashboard.html`:
    - Mengubah cabang `stateContext.botPhase === 'PENDING_PAUSE'` pada `getOutletPauseLine` agar mengembalikan teks ringkas `"Bot sedang dalam proses penutupan outlet"`.
    - Mengubah cabang `stateContext.botPhase === 'PENDING_OPEN'` saat berada dalam jadwal operasional agar mengembalikan `"Bot sedang dalam proses pembukaan outlet"`.
  - `tests/test_dashboard_label_contract.py`:
    - Menambahkan unit test kontrak `test_mitra_pending_pause_and_open_subtext_contract`.

## Handling
- Menghilangkan redundansi keterangan waktu buka kembali yang sebelumnya bertumpuk saat permintaan tutup masih menunggu antrean eksekusi bot.
