# Update v1.1.9

## Whats New

- Label action bot sekarang menampilkan satu indikator `Bot` tanpa teks `(Bot)` yang berulang.
- Jarak label waktu dan informasi close dirapikan.

## Spesifikasi

- Action open: `Bot` + `Buka Otomatis` + nama outlet, lalu pill waktu action.
- Action close: `Bot` + `Tutup Otomatis` + nama outlet, lalu pill waktu action dan pill target buka.

## Handling

- Sumber data dan filter owner tidak berubah.
- Tidak ada perubahan pada request action bot maupun endpoint history.
