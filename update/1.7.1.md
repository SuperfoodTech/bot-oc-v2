# v1.7.1

## Whats New

- Memperbaiki URL sumber import Virtual Brand ke tab Google Sheet
  `gid=401458905`.

## Spesifikasi

- Endpoint CSV VB sekarang mengembalikan data matrix Virtual Brand yang valid.
- Filter kolom `Status` tetap hanya menerima nilai `Aktif`, konsisten dengan
  gate Bot O/C.

## Handling

- Fetch VB kembali memakai endpoint `/api/v1/admin/vb/import`.
- Jika tab atau published URL berubah lagi, validasi harus dilakukan dengan
  memastikan response berstatus HTTP 200 dan `Content-Type` CSV.
