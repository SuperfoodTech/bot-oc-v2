# v1.10.1

## Whats New

- Mengembalikan konfigurasi browser `bot-vb` ke mode headless untuk server.

## Spesifikasi

- `docker-compose.yml` menetapkan `HEADLESS=true` untuk `bot-vb`.
- Mode non-headless tidak digunakan dalam deployment Docker.

## Handling

- Mencegah error `DevToolsActivePort file doesn't exist` pada server tanpa
  display/X server.
- Eksperimen lokal `HEADLESS=false` tidak lagi menjadi konfigurasi rilis.
- Worker dan bot-OC tidak diubah.
