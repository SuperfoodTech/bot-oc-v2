# v1.6.4

## Whats New

- Memperbaiki kegagalan fitur "Fetch Data VB" (impor matriks Virtual Brand) dari Google Sheet.
- Mengatasi duplikasi mapping brand pada outlet yang menyebabkan error database (`psycopg.errors.UniqueViolation`).

## Spesifikasi

- Memperbarui GID sheet untuk Virtual Brand dari `2099001096` ke GID yang baru aktif `401458905` pada `src/backend/vb.py` dan `main-vb/src/importer.py`.
- Mengubah klausul konflik database pada tabel `vb_brand_outlets` menjadi `ON CONFLICT (outlet_id)` dan memperbarui kolom `vb_brand_id` ke brand yang baru diset jika terjadi re-assignment.

## Handling

- Setelah perubahan diterapkan, web service backend telah di-rebuild dan direstart tanpa downtime menggunakan:
  ```bash
  docker compose build web
  docker compose up -d --no-deps web
  ```
