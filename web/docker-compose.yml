services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fm-web
    restart: always
    ports:
      - "8080:8080"
    environment:
      - ENVIRONMENT=development
      - PORT=8080
      - ALLOWED_USERNAMES=auto7313
    volumes:
      - ./src:/app/src
