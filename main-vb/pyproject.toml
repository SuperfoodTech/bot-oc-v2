[project]
name = "foodmaster-main-vb"
version = "0.1.0"
description = "FoodMaster Virtual Brand patrol service"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "psycopg[binary]>=3.2.0",
    "requests>=2.33.1",
    "selenium",
    "webdriver-manager",
    "uvicorn[standard]>=0.34.0",
]

[tool.uv]
package = false
